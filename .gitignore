__pycache__/
*.pyc
*.egg-info/
build/
dist/
.venv/
salida/
output/
# Evidencia de QA (ADR 0019): el bulto se ignora; el README de la convencion y
# la evidencia CITADA desde HANDOFF/CHANGELOG se commitean (git add -f).
qa_runs/*
!qa_runs/README.md
reference/
driver/
*.ld
*.ldx
# .claude/ viaja con el repo (skills, hooks, settings.json); solo lo local se ignora:
.claude/settings.local.json
.claude/.review-marker
.claude/.mariana-marker
CLAUDE.md
docs/demo/_*
.obsidian/
.nicegui/

# Artefacto de build: nicegui-pack regenera el .spec en cada corrida (ruta absoluta local)
SimGhostInputs.spec
